### ٢٣ يوليو ٢٠٢٦
- تم استبدال المحرك الخلفي لفصل الصوت والمرافقة من UVR5 إلى PyMSS، مع دعم إيقاف الفصل أثناء الاستدلال (خوارزمية فصل صوت أفضل يمكنها تحسين جودة الاستدلال بشكل ملحوظ).
- تم تحسين تحميل الصوت المدخل وإعادة أخذ العينات لاستخدام معالجة GPU عندما يكون ذلك ممكنًا، مما يحسن كفاءة الاستدلال ويقلل استخدام CPU.
- تم إصلاح خطأ CUDA Graph الذي كان يظهر عند تمكين تقليل ضوضاء الإدخال أو الإخراج أثناء تحويل الصوت في الوقت الفعلي.
- تم إصلاح توافق libuv لعمليات التدريب على نظام Windows.

### ١٨ يوليو ٢٠٢٦
- تم تقليل تعقيد واجهة المستخدم الرسومية (لم يعد مسار الإدخال مطلوبًا لإدخال الصوت في الاستدلال، وتحسين سجلات الواجهة الأمامية في الوقت الفعلي، وتحديث قائمة النماذج تلقائيًا بعد حفظ نموذج استدلال صغير، والكشف التلقائي عن مسار الفهرس المطابق بعد اختيار النموذج في صفحة الاستدلال الرئيسية، إلخ).
- أصبح بإمكان استخراج ميزات البيانات وتدريب النموذج وبناء الفهرس الآن التوقف والاستئناف بعد إعادة التشغيل.
- يكتشف تلقائيًا وحدة معالجة الرسومات لتحديد وضع ودقة الاستدلال.
- تم إصلاح مشكلات التبعيات (بما في ذلك مشكلات pyworld، fairseq، matplotlib، PyAV، وبيئة ONNX Runtime GPU).
- تم حل مشكلات الترميز.
- تبحث واجهة الويب الآن تلقائيًا عن منفذ متاح عند بدء التشغيل.
- لم يعد تدريب وحدة GPU الواحدة يستخدم DDP.
- يظل النموذج الأساسي دون تغيير. تم إصلاح العديد من المشكلات التي تؤثر على جودة المخرجات. قد يؤدي تحديث الحزمة/الكود المضمن وإعادة تشغيل الاستدلال بنموذج معدل موجود، أو إعادة التدريب ثم تشغيل الاستدلال بالإصدار الجديد، إلى تحسين النتائج.
- تمت إضافة دعم لخوارزمية استخراج النغمة FCPE.
- تدعم واجهة تحويل الصوت في الوقت الفعلي تسريع استدلال CUDA Graph، مما يحقق تسريعًا يصل إلى 4.7 ضعفًا في زمن استدلال الخوارزمية (تم الاختبار على RTX 4090D)، بينما يظل استخدام GPU حوالي 20-30٪.
- تمت إزالة الميزات التي أصبحت قديمة أو غير مفيدة بوضوح.

### ٦ أكتوبر ٢٠٢٣
- قمنا بإنشاء واجهة رسومية لتغيير الصوت في الوقت الفعلي: `go-realtime_gui.bat` / `realtime_gui.py` (لاحظ أنه يجب عليك اختيار نفس نوع جهاز الإدخال والإخراج، مثل MME و MME).
- قمنا بتدريب نموذج RMVPE أفضل لاستخراج النغمة.
- تحسين تخطيط واجهة الاستدلال الرسومية.

### ١٣ أغسطس ٢٠٢٣

**١- إصلاحات الأخطاء المنتظمة**
- تغيير الحد الأدنى لعدد الحقبات الكلية إلى 1، وتغيير الحد الأدنى لعدد الحقبات الكلية إلى 2.
- إصلاح أخطاء التدريب عند عدم استخدام النماذج المدربة مسبقًا.
- بعد فصل الصوت والمرافقة، يتم مسح ذاكرة الرسومات.
- تغيير مسار حفظ faiss من المسار المطلق إلى المسار النسبي.
- دعم المسارات التي تحتوي على مسافات (كل من مسار مجموعة التدريب واسم التجربة مدعومان، ولن يتم الإبلاغ عن أخطاء).
- إلغاء إلزامية ترميز UTF-8 لقائمة الملفات.
- حل مشكلة استهلاك CPU الناتجة عن بحث faiss أثناء تغيير الصوت في الوقت الفعلي.

**٢- التحديثات الرئيسية**
- تدريب أقوى نموذج مفتوح المصدر حاليًا لاستخراج نغمة الصوت RMVPE، واستخدامه في تدريب RVC والاستدلال غير المتصل/في الوقت الفعلي، مع دعم PyTorch / Onnx / DirectML.
- دعم بطاقات الرسومات AMD و Intel عبر Pytorch_DML.

(١) تغيير الصوت في الوقت الفعلي (٢) الاستدلال (٣) فصل الصوت والمرافقة (٤) التدريب غير مدعوم حاليًا، وسيتم التبديل إلى التدريب على CPU؛ يدعم استدلال RMVPE لوحدة معالجة الرسومات عبر Onnx_Dml.

### ١٨ يونيو ٢٠٢٣
- نماذج مدربة مسبقًا جديدة v2: 32k و 48k.
- إصلاح أخطاء استدلال النماذج غير f0.
- لمجموعات التدريب التي تتجاوز ساعة واحدة، يتم إجراء `minibatch-kmeans` تلقائيًا لتقليل شكل الميزات، مما يجعل تدريب الفهرس وإضافته والبحث فيه أسرع بكثير.
- توفير مساحة `toy vocal2guitar` على Hugging Face.
- حذف تلقائي لمقاطع التدريب القصيرة الشاذة.
- علامة تبويب تصدير Onnx.

**تجارب فاشلة:**
- ~~استرجاع الميزات: إضافة استرجاع الميزات الزمنية: غير فعال~~
- ~~استرجاع الميزات: إضافة تقليل الأبعاد PCAR: البحث أبطأ~~
- ~~زيادة البيانات العشوائية أثناء التدريب: غير فعال~~

**قائمة المهام:**
- ~~Vocos-RVC (مشفر صوتي صغير): غير فعال~~
- ~~دعم Crepe للتدريب: تم استبداله بـ RMVPE~~
- ~~استدلال Crepe بنصف الدقة: تم استبداله بـ RMVPE. ومن الصعب تحقيقه.~~
- دعم محرر F0.

### ٢٨ مايو ٢٠٢٣
- إضافة دفتر ملاحظات Jupyter v2، وسجل التغييرات بالكورية، وإصلاح بعض متطلبات البيئة.
- إضافة وضع حماية الحروف الساكنة غير المجهورة وأصوات التنفس.
- دعم كشف النغمة الكامل `crepe-full`.
- فصل الصوت UVR5: دعم نماذج إزالة الصدى (`dereverb`) ونماذج إزالة الصدى (`de-echo`).
- إضافة اسم التجربة والإصدار على اسم الفهرس.
- دعم المستخدمين لتحديد تنسيق تصدير الملفات الصوتية الناتجة يدويًا عند معالجة تحويل الصوت بالدفعة وفصل الصوت UVR5.
- لم يعد تدريب نموذج v1 32k مدعومًا.

### ١٣ مايو ٢٠٢٣
- تنظيف الأكواد الزائدة من الإصدار القديم في الحزمة المضغوطة: `lib.infer_pack` و `uvr5_pack`.
- إصلاح خطأ المعالجة المتعددة الزائفة في معالجة مجموعة التدريب المسبقة.
- إضافة ضبط نصف قطر الترشيح الوسيط لخوارزمية كشف النغمة `harvest`.
- دعم إعادة أخذ العينات في مرحلة ما بعد المعالجة لتصدير الصوت.
- تم تغيير إعداد "n_cpu" للمعالجة المتعددة للتدريب من "استخراج F0" إلى "معالجة البيانات المسبقة واستخراج F0".
- الكشف التلقائي عن مسارات الفهرس ضمن مجلد `logs` وتوفير وظيفة قائمة منسدلة.
- إضافة "أسئلة وأجوبة شائعة" في صفحة التبويب (يمكنك أيضًا الرجوع إلى ويكي RVC على GitHub).
- عند الاستدلال، يتم تخزين نغمة `harvest` مؤقتًا عند استخدام نفس مسار الصوت المدخل (الغرض: استخدام استخراج نغمة `harvest`، ستمر العملية بأكملها بعملية استخراج نغمة طويلة ومتكررة. إذا لم يتم استخدام التخزين المؤقت، فإن المستخدمين الذين يجربون أجراسًا وفهارس وإعدادات مختلفة لنصف قطر الترشيح الوسيط سيعانون من عملية انتظار مؤلمة بعد الاستدلال الأول).

### ١٤ مايو ٢٠٢٣
- استخدام غلاف حجم الصوت المدخل لمزج أو استبدال غلاف حجم الصوت المخرج (يمكن أن يخفف من مشكلة "كتم الإدخال وضوضاء السعة الصغيرة في الإخراج". إذا كانت ضوضاء الخلفية للصوت المدخل عالية، لا يُنصح بتشغيلها، وهي غير مفعلة افتراضيًا (يمكن اعتبار ١ غير مفعل)).
- دعم حفظ النماذج الصغيرة المستخرجة بتردد محدد (إذا كنت تريد رؤية الأداء في حقبات مختلفة، ولكنك لا تريد حفظ جميع نقاط التفتيش الكبيرة واستخراج النماذج الصغيرة يدويًا عبر `ckpt-processing` في كل مرة، ستكون هذه الميزة عملية جدًا).
- حل مشكلة "أخطاء الاتصال" الناتجة عن الوكيل العالمي للخادم عن طريق تعيين متغيرات البيئة.
- دعم النماذج المدربة مسبقًا v2 (حاليًا، تتوفر إصدارات 40k فقط للاختبار العام، ولم يتم تدريب معدلي العينة الآخرين بالكامل بعد).
- تقييد الحجم المفرط الذي يتجاوز ١ قبل الاستدلال.
- تم تعديل إعدادات معالجة مجموعة التدريب المسبقة بشكل طفيف.

---

### سجلات التغييرات التاريخية:

### ٩ أبريل ٢٠٢٣
- إصلاح معلمات التدريب لتحسين معدل استخدام GPU: ارتفع A100 من ٢٥٪ إلى حوالي ٩٠٪، V100: من ٥٠٪ إلى حوالي ٩٠٪، 2060S: من ٦٠٪ إلى حوالي ٨٥٪، P40: من ٢٥٪ إلى حوالي ٩٥٪؛ تحسين كبير في سرعة التدريب.
- تغيير المعلمة: أصبح حجم الدفعة الإجمالي الآن هو حجم الدفعة لكل GPU.
- تغيير `total_epoch`: تم رفع الحد الأقصى من ١٠٠ إلى ١٠٠٠؛ وتم رفع الحد الافتراضي من ١٠ إلى ٢٠.
- إصلاح مشكلة استخراج `ckpt` التي كانت تتعرف على النغمة بشكل خاطئ، مما تسبب في استدلال غير طبيعي.
- إصلاح مشكلة حفظ `ckpt` لكل رتبة في التدريب الموزع.
- تطبيق تصفية الميزات `NaN` لاستخراج الميزات.
- إصلاح مشكلة إنتاج الإدخال/الإخراج الصامت لأصوات ساكنة عشوائية أو ضوضاء (يجب إعادة تدريب النماذج القديمة باستخدام مجموعة بيانات جديدة).

### ١٦ أبريل ٢٠٢٣ - تحديث
- إضافة واجهة رسومية صغيرة لتغيير الصوت في الوقت الفعلي محليًا، ابدأ بالنقر المزدوج على `go-realtime_gui.bat`.
- تطبيق تصفية للنطاقات الترددية أقل من ٥٠ هرتز أثناء التدريب والاستدلال.
- خفض الحد الأدنى لاستخراج نغمة `pyworld` من ٨٠ الافتراضي إلى ٥٠ للتدريب والاستدلال، مما يسمح للأصوات الذكورية المنخفضة بين ٥٠-٨٠ هرتز بعدم الكتم.
- تدعم واجهة الويب تغيير اللغات وفقًا للإعدادات المحلية للنظام (تدعم حاليًا `en_US`، `ja_JP`، `zh_CN`، `zh_HK`، `zh_SG`، `zh_TW`؛ الافتراضي هو `en_US` إذا لم يكن مدعومًا).
- إصلاح التعرف على بعض وحدات معالجة الرسومات (مثل فشل التعرف على V100-16G، وفشل التعرف على P4).

### ٢٨ أبريل ٢٠٢٣ - تحديث
- ترقية إعدادات فهرس faiss لسرعة أعلى وجودة أفضل.
- إزالة الاعتماد على `total_npy`؛ لن يتطلب مشاركة النموذج في المستقبل إدخال `total_npy`.
- فتح القيود المفروضة على وحدات معالجة الرسومات من السلسلة ١٦، وتوفير إعدادات استدلال 4GB لوحدات GPU بسعة 4GB VRAM.
- إصلاح خطأ في فصل الصوت والمرافقة UVR5 لبعض تنسيقات الصوت.
- تدعم واجهة تغيير الصوت في الوقت الفعلي الآن النماذج غير 40k والنماذج غير `lazy pitch`.

### الخطط المستقبلية:

**الميزات:**
- إضافة خيار: استخراج نماذج صغيرة لكل حفظ حقبة.
- إضافة خيار: تصدير mp3 إضافي إلى المسار المحدد أثناء الاستدلال.
- دعم علامة تبويب تدريب متعدد الأشخاص (حتى ٤ أشخاص).

**النموذج الأساسي:**
- جمع ملفات صوت التنفس لإضافتها إلى مجموعة بيانات التدريب لإصلاح مشكلة تشويه أصوات التنفس.
- نقوم حاليًا بتدريب نموذج أساسي باستخدام مجموعة بيانات غناء موسعة، وسيتم إصدارها في المستقبل.
